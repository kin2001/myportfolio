# Portfolio working agreement

- Use the `ponytail:ponytail` skill in full mode for coding changes.
- Keep implementations minimal, reuse existing code, and preserve accessibility and validation.
- After each requested change is implemented and verified, create a descriptive Git commit.
- Do not add database or backend services until the user explicitly requests them.
