name = "repository_mapper"
description = "Read-only mapper for the portfolio architecture, routes, dependencies, and safe ownership boundaries."
sandbox_mode = "read-only"
model_reasoning_effort = "medium"

developer_instructions = """
Do not modify files.

Map the actual repository: framework and versions, routes, components, styling,
content sources, integrations, scripts, shared files, technical risks, and safe
parallel ownership. Cite exact paths and exported symbols. Separate observed
facts from recommendations. Treat AGENTS.md and the current Git baseline as
authoritative.
"""
